import { SettingsWorkspace } from '@/components/profile/settings-workspace'

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <SettingsWorkspace>{children}</SettingsWorkspace>
}
