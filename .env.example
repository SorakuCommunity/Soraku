# ============================================================
# SORAKU COMMUNITY — .env.example — v1.0.a3.5
# Copy ke .env.local dan isi dengan nilai asli kamu.
# Jangan pernah commit .env.local ke Git!
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here   # server-side only

# ── Discord OAuth + Bot ───────────────────────────────────────
DISCORD_CLIENT_ID=your_discord_oauth_client_id
DISCORD_CLIENT_SECRET=your_discord_oauth_client_secret
DISCORD_BOT_TOKEN=your_discord_bot_token              # for guild member API
DISCORD_GUILD_ID=your_discord_server_id               # e.g. 1234567890
DISCORD_INVITE_URL=https://discord.gg/your-invite     # shown on hero card

# ── Google OAuth ──────────────────────────────────────────────
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ── Supabase Storage ──────────────────────────────────────────
# Create a public bucket named "gallery" in Supabase Storage dashboard
# Policy: allow INSERT for authenticated users, SELECT for all
NEXT_PUBLIC_SUPABASE_STORAGE_URL=https://your-project.supabase.co/storage/v1

# ── Spotify (optional — for Now Playing widget) ───────────────
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REFRESH_TOKEN=your_spotify_refresh_token

# ── GitHub (optional — for community discussions on /komunitas) ─
GITHUB_OWNER=SorakuCommunity
GITHUB_REPO=Soraku
GITHUB_TOKEN=ghp_your_fine_grained_token   # public repo discussions

# ── Redis (optional — for rate limiting in API routes) ────────
REDIS_URL=redis://localhost:6379
# or Upstash: redis://default:password@endpoint.upstash.io:6379

# ── App ───────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3000   # change to your domain in prod
NEXT_PUBLIC_SITE_NAME=Soraku Community
