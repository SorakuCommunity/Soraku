# Nginx Reverse Proxy Configuration

## Domains
- soraku.id → apps/web (port 3000)
- stream.soraku.id → apps/stream (port 3001)
- api.soraku.id → services/api (port 4000)

## SSL
- Let's Encrypt certificates
- Auto-renewal via certbot
